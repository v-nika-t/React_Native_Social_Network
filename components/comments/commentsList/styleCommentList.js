import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textUserName: {
        fontWeight: 'bold'
    },
    titleText: {
        fontSize: 20,
        fontWeight: "bold"
    },
    container: {
        flex: 1,
        color: 'red'
    }
});

module.exports = styles;