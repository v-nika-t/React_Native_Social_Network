import { View, TextInput } from 'react-native';

const SearchPanel = () => {
    return (
        <View>
            <TextInput
                placeholder='Поиск'
            />
        </View>
    )
}
export default SearchPanel;